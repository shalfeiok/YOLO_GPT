"""Application-layer facades.

UI should depend on the application layer (use-cases/facades) and not on
infrastructure implementations from :mod:`app.services` directly.

These facades intentionally provide a thin, stable API surface.
"""
